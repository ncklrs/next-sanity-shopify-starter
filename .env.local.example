# Sanity Configuration
# Get these values from https://sanity.io/manage
NEXT_PUBLIC_SANITY_PROJECT_ID=your-project-id
NEXT_PUBLIC_SANITY_DATASET=production

# API Tokens - Create at https://sanity.io/manage/project/YOUR_PROJECT_ID/api#tokens
# Read Token: For preview mode and fetching draft content (Viewer permission)
SANITY_API_READ_TOKEN=

# Write Token: For form submissions and mutations (Editor permission)
# Only used server-side - never expose to client
SANITY_API_WRITE_TOKEN=

# Webhook Secret for On-Demand Revalidation
# Generate a random string: openssl rand -hex 32
# Configure this same secret in Sanity webhook settings at:
# https://sanity.io/manage/project/YOUR_PROJECT_ID/api#webhooks
SANITY_WEBHOOK_SECRET=

# ============================================================================
# Form Action Email Providers (choose one or configure multiple)
# ============================================================================

# Resend (recommended) - https://resend.com
# Create an API key at https://resend.com/api-keys
RESEND_API_KEY=

# Email From Address (required for email notifications)
# Must be verified domain or use Resend's shared domain
EMAIL_FROM=forms@yourdomain.com

# SendGrid (alternative) - https://sendgrid.com
# Create an API key at https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=

# Mailgun (alternative) - https://mailgun.com
# Get API key from https://app.mailgun.com/settings/api_security
MAILGUN_API_KEY=
MAILGUN_DOMAIN=mg.yourdomain.com

# ============================================================================
# Shopify Integration
# ============================================================================

# Shopify Storefront API
# Get these from: Shopify Admin > Apps > Develop apps > Create custom app
# Enable Storefront API and create access token
NEXT_PUBLIC_SHOPIFY_STORE_DOMAIN=your-store.myshopify.com
NEXT_PUBLIC_SHOPIFY_STOREFRONT_ACCESS_TOKEN=your-storefront-access-token

# Optional: Shopify Admin API (for product sync)
# Enable Admin API scopes: read_products, read_product_listings
SHOPIFY_ADMIN_ACCESS_TOKEN=your-admin-access-token
